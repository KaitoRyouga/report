version: "3.8"

services:
  frontend:
    restart: always
    build: 
        context: ./frontend
        dockerfile: Dockerfile.dev
    ports:
    - "80:3000"
    volumes: 
    - ./frontend:/app
  backend:
    restart: always
    build: 
        context: ./backend
        dockerfile: Dockerfile
    ports:
    - "3001:3001"